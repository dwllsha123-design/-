# Storefront — دار الأنوثة

واجهة المتجر الإلكتروني للعملاء (منفصلة عن Admin).

## التشغيل

```bash
cd storefront
npm install
npm run dev
```

يفتح على: http://localhost:5174

يتطلب تشغيل الـ Backend على المنفذ 3000.
