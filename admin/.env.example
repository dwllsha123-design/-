# Copied into the client at build time (`npm run build` reads `.env.production`).
# Leave as /api/v1 when Nginx on the same domain proxies to the API.
VITE_API_URL=/api/v1
# Production path for the admin app behind Nginx
VITE_BASE=/admin/
